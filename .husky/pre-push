echo "🚀 Running pre-push checks..."

# Run eslint on entire codebase
npm run lint || exit 1

# Run prettier check (no auto-fix, just verify)
npx prettier --check "**/*.{ts,js,json,md,scss,html,css}" || exit 1

# Optionally, run tests
npm test || exit 1

echo "✅ Pre-push checks passed!"
